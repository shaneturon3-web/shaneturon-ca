import { enCaseFiles } from './case-files';
import { enLab } from './lab';
import { enNow } from './now';
import { enContact } from './contact';
import { enArchitecture } from './architecture';
import { enHome } from './home';
import { enAbout } from './about';
import { enSystems } from './systems';
import { enWriting } from './writing';

export const enPages = {
  caseFiles: enCaseFiles,
  lab: enLab,
  now: enNow,
  contact: enContact,
  architecture: enArchitecture,
  home: enHome,
  about: enAbout,
  systems: enSystems,
  writing: enWriting,
};
